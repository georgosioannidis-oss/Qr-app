import { RestaurantOnboardingForm } from "@/components/RestaurantOnboardingForm";

/** Gated new-restaurant signup — not linked in the app. Access password required (see `.env.example`). */
export default function SignupPage() {
  return <RestaurantOnboardingForm />;
}
